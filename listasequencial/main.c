#include <stdio.h>
#include <stdlib.h>
#include "listasequencial.h"

int main()
{
    printf("Hello world!\n");
    Lista *li;
    li=criar_lista();

    int x=tamanho_lista(li);
    int y=lista_cheia(li);
    if(lista_cheia(li)){}
    int z;
    z = lista_vazia(Lista* li);
    if(lista_vazia(li))




    return 0;

}
